// Example from unit 4.5
class Main {

    static boolean test;
    field int a, b;

    function void main(int a, int b) {
        var int var1, var2;
        var char c1;

        if ( var1 ) {
            let count = 500;
        } else {
            let count = 700;
        }

        do foo(5);
        do Main.foo();

        // terms: let x = term
        let x = 5;      // integerConstant (= term)
        let x = "st";   // stringConstant
        let x = true;   // keywordConstant
        let x = -5;     // unaryOp term
        let x = (5);    // '(' expression ')'

        let x = var1;   // varName (identifier)
        let x = arr[5]; // varName '[' expression ']'
        let x = Main.foo();  // subroutineCall

        return;
    }

    function void foo(){
        var int i, j;
        var Array a;

        let a[i] = j;
        return;
    }
}
